#include "stack.h"

/*
* f2 simply calls the print_stack() function.
* @param *input_str: Takes a string as an input.
* Function has a void return value.
*/

void f2(char *input_str) {
    print_stack("in f2", OFFSET);
}
